import { CourseDescriptionLengthDirective } from './course-description-length.directive';

describe('CourseDescriptionLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseDescriptionLengthDirective();
    expect(directive).toBeTruthy();
  });
});
