import { NumberSegmentDirective } from './number-segment.directive';

describe('NumberSegmentDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberSegmentDirective();
    expect(directive).toBeTruthy();
  });
});
