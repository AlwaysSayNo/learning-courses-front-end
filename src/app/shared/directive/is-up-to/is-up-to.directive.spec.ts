import { IsUpToDirective } from './is-up-to.directive';

describe('IsUpToDirective', () => {
  it('should create an instance', () => {
    const directive = new IsUpToDirective();
    expect(directive).toBeTruthy();
  });
});
