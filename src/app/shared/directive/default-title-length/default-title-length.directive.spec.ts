import { DefaultTitleLengthDirective } from './default-title-length.directive';

describe('CourseMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultTitleLengthDirective();
    expect(directive).toBeTruthy();
  });
});
