import { CourseTitleLengthDirective } from './course-title-length.directive';

describe('CourseMaxLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseTitleLengthDirective();
    expect(directive).toBeTruthy();
  });
});
